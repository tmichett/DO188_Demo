apiVersion: v1
kind: Service
metadata:
  name: demo-web-pod 
  labels:
    app: demo-web-pod
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: demo-web-pod
